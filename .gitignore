# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Internal build-tool planning notes (not part of the product)
.lovable/

# .env já commitado hoje só tem valores públicos (VITE_SUPABASE_URL/PUBLISHABLE_KEY,
# que vão pro bundle do cliente de qualquer forma) — mas evita que um secret de
# verdade entre aqui por hábito daqui pra frente.
.env
.env.local
